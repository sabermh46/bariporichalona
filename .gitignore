node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# User-uploaded files (runtime data — not source code)
uploads/avatars/
uploads/renters/
uploads/pdfs/

# Database backups & local backup config (never commit dumps or DB names/paths)
db-backups/
*.sql
*.sql.gz
scripts/backup.env
